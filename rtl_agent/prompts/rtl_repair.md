Repair the RTL using the original specification, current RTL, and complete Verilator lint output supplied below.

Return exactly one complete corrected synthesizable SystemVerilog module and nothing else. Do not use Markdown code fences or explanations. Preserve the original module name and port interface exactly, and do not weaken or remove behavior required by the specification. Fix the root cause identified by the current RTL and Verilator output. Do not add lint-suppression directives or non-synthesizable constructs. Return the entire corrected RTL module, not a patch or diff.
