Generate exactly one complete SystemVerilog self-checking testbench module and return only its source code. Do not return Markdown fences, explanations, patches, or diffs.

Keep the requested testbench module name and instantiate the DUT using its exact module name and public port interface. Do not change the DUT or access DUT internal signals through hierarchical references. Execute every required test-plan scenario using deterministic tests. At each check, compare expected and actual values; on mismatch print an informative error including TEST_FAIL and call `$fatal(1)`. After every check succeeds, print exactly TEST_PASS and call `$finish`. Add a finite timeout watchdog that prints TEST_FAIL and terminates with `$fatal(1)` to prevent an infinite simulation.

The testbench need not be synthesizable. Do not use UVM, classes, DPI, or external file dependencies. Use syntax supported by Verilator `--binary --timing`. Do not weaken the specification, omit tests, or modify the DUT to make tests pass.

For every required `TP_*` testcase ID, declare exactly one named `task automatic run_<ID>` and call it exactly once from the main initial flow. Inside that task, include each literal exactly once: `TESTCASE_BEGIN: <ID>`, `TESTCASE_PASS: <ID>`, and `TESTCASE_FAIL: <ID>`. The task must perform its real preconditions, stimulus, observation timing, and independent expected comparison. Route all comparison failures in that testcase to one reachable failure section that prints its single TESTCASE_FAIL literal immediately before `$fatal(1)`; do not rely on a shared fail helper and do not repeat the marker for multiple comparisons. Do not place FAIL in an unreachable block, print markers as a substitute for checks, or derive expected values from DUT output. Print global TEST_PASS only after every testcase task succeeds. Keep global TEST_FAIL behavior consistent with the simulator and use Verilator `--binary --timing` compatible syntax.

When the specification distinguishes synchronous from asynchronous reset, test that timing semantics explicitly: first establish a non-reset output value, assert reset away from the active clock edge, check that a synchronous-reset DUT does not change before the next active edge, and then check the reset value after that edge. Apply the converse timing check for a specified asynchronous reset.
